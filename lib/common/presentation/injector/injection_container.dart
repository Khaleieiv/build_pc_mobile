import 'package:flutter/material.dart';

class InjectionContainer extends StatelessWidget {
  const InjectionContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
